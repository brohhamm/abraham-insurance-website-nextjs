import type { MetadataRoute } from "next"; import { articles } from "@/content/articles"; import { siteConfig } from "@/lib/site-config";
export default function sitemap():MetadataRoute.Sitemap{const paths=["","/personal-insurance","/business-insurance","/carriers","/wildfire-insurance","/referral-partners","/education","/about","/contact","/privacy","/terms"]; return [...paths.map(path=>({url:`${siteConfig.url}${path}`,lastModified:new Date(),changeFrequency:path===""?"weekly" as const:"monthly" as const,priority:path===""?1:.7})),...articles.map(a=>({url:`${siteConfig.url}/education/${a.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.6}))]}
