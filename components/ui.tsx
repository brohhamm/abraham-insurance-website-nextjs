import Link from "next/link";
export function PageHero({ eyebrow, title, children }: { eyebrow: string; title: string; children: React.ReactNode }) { return <section className="page-hero"><div className="shell narrow"><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="lead">{children}</p></div></section>; }
export function CTA() { return <section className="cta"><div className="shell cta-inner"><div><p className="eyebrow light">Clear next steps</p><h2>Let’s review your insurance needs.</h2><p>Receive responsive guidance and coverage options based on your situation.</p></div><div className="button-row"><Link className="button button-light" href="/contact">Request a Quote</Link><a className="button button-outline-light" href="tel:+17143889533">Call 714-388-9533</a></div></div></section>; }
export function Cards({ items }: { items: readonly { title: string; text: string }[] }) { return <div className="card-grid">{items.map(x => <article className="card" key={x.title}><div className="card-mark" aria-hidden="true">✓</div><h3>{x.title}</h3><p>{x.text}</p><Link className="card-link" href={`/contact?insurance=${encodeURIComponent(x.title)}`}>Request a {x.title} Quote →</Link></article>)}</div>; }
