import { notFound } from "next/navigation"; import Link from "next/link"; import { articles, getArticle } from "@/content/articles";
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const a=getArticle(slug); return a?{title:a.title,description:a.summary}:{title:"Article"}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const a=getArticle(slug); if(!a) notFound(); return <article className="article-page"><div className="shell article-body"><Link className="back-link" href="/education">← Education Center</Link><p className="eyebrow">Insurance guide</p><h1>{a.title}</h1><p className="article-summary">{a.summary}</p>{a.body.map(p=><p key={p}>{p}</p>)}<div className="article-disclaimer"><strong>Important:</strong> This information is educational and does not modify any policy. Coverage is subject to eligibility, underwriting, policy terms, conditions, limitations, and exclusions. Actual policy language controls.</div><Link className="button" href="/contact">Ask About Your Coverage</Link></div></article>}
